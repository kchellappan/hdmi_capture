__pycache__/
*.pyc
.cache/

# C++ build output
vcap_cpp/build/
vcap_cpp/build-fb/
compile_commands.json

# Recordings: these are training data, often many gigabytes, and are outputs rather than
# sources. At 1080p60 on live content a session is ~52 GB/hour.
data/
*.mjpg
*.idx

# Exports
*.mp4
*.mkv
